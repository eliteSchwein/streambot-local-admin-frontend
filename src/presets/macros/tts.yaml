tasks:
  - channel: condition
    method: if
    check: '''${data.text}'' === '''''
  - channel: function
    method: send_message
    data:
      content: Du musst einen Text angeben!
  - channel: condition
    method: end_macro
  - channel: condition
    method: end_if
  - channel: dummy_alert
    message: ${context.userName} sagt ${data.text}
    duration: tts
    icon: ''
    speak: true
    event_uuid: ${eventUuid}
